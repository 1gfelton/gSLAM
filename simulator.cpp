#include <bits/stdc++.h>
#include <landmark.h>
using namespace std;

int main() {
    // i want a world
        // in the world i need to be able to place landmarks (points)
        // i also need a robot
        // the robot needs to be able to move
        // the robot needs to be able to measure where the landmarks are in relation to it and the landmarks around it
    return 0;
}